class Defaults {
    companion object {
        val substitution = mapOf(
            "CoreModule" to "core-module"
        )
        val using = mapOf(
            "CoreModule" to "app"
        )
    }
}